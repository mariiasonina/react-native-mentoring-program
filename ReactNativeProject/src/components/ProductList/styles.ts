import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  loader: {
    marginVertical: 10,
  },
  columnWrapper: {
    padding: 10,
    gap: 20,
    width: '50%',
  },
  productList: {
    padding: 10,
  },
});
